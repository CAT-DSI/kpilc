import { CorporateCodesDirective } from './corporate-codes.directive';

describe('CorporateCodesDirective', () => {
  it('should create an instance', () => {
    const directive = new CorporateCodesDirective();
    expect(directive).toBeTruthy();
  });
});
