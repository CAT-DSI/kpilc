import { CustomTableColumn } from './custom-table-column.interface';

describe('CustomTableColumn', () => {
  it('should create an instance', () => {
    expect(new CustomTableColumn()).toBeTruthy();
  });
});
